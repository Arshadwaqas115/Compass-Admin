export const roomOptions = ['single', 'double', 'triple'].map((item) => {
  return {
    value: item,
    label: item,
  };
});

export const hotelOptions = ['HotelOne', 'HotelTwo', 'HotelThree'].map((item) => {
  return {
    value: item,
    label: item,
  };
});

export const vehicleOptions = ['car', 'hiace', 'coaster'].map((item) => {
  return {
    value: item,
    label: item,
  };
});

export const sectorOptions = ['airlines', 'road', 'rail'].map((item) => {
  return {
    value: item,
    label: item,
  };
});

export const mealOptions = ['RO', 'BB', 'HB', 'FB', 'Iftar', 'Sahur'].map((item) => {
  return {
    value: item,
    label: item,
  };
});

export const serviceOptions = ['dummy1', 'dummy2', 'dummy3'].map((item) => {
  return {
    value: item,
    label: item,
  };
});

export const staffOptions = ['staff1', 'staff2', 'staff3'].map((item) => {
  return {
    value: item,
    label: item,
  };
});
